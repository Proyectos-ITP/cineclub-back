# Redis configuration for ACL with user authentication

# Enable ACL (file will be generated dynamically)
aclfile /data/users.acl

# Bind to all interfaces
bind 0.0.0.0

# Disable protected mode for development
protected-mode no

# Persistence
save 900 1
save 300 10
save 60 10000

# Logging
loglevel notice
